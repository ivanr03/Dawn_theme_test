<!doctype html>
<html lang="{{ shop.locale }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ page_title }}</title>
    {{ content_for_header }}
  </head>
  <body>
    <header>
      <div style="text-align: center; padding: 20px;">
        <h1>Your Centered Header Text</h1>
      </div>
    </header>

    {{ content_for_layout }}
  </body>
</html>
